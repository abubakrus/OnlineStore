<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OnlineStore" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="mainBackgroundColor">@color/white</item>
        <item name="categoriesBackgroundColor">@color/white</item>
        <item name="textColor">@color/black</item>
        <item name="basket_icon">@drawable/basket_icon</item>
        <item name="like_icon">@drawable/like_button</item>
        <item name="search_icon">@drawable/search_view_icon</item>
        <item name="searchViewBackgroundColor">@color/white</item>
        <item name="basket_card_background">@color/white</item>
        <item name="android:statusBarColor">@android:color/white</item>
=    </style>

    <style name="Theme.OnlineStore" parent="Base.Theme.OnlineStore" />

</resources>